[Omschrijving]

Fase 2 van het project omvat een GUI gemaakt in Qt. Om deze te builden is Qt creator vereist.
Het project kan vervolgens gebuild worden door MMM_GUI.pro te openen met Qt creator en op de build knop te drukken.

[Codestructuur]

	Fase2
	|-- Grammars			::Grammatica's voor MusicXML & MEI
	|-- MMM_GUI			::Bevat de main voor de programmacode
	|	|-- backEndCode		::Code van Fase1 die gebruikt wordt in het programma, deze is enorm uitgebreid tegenover die van fase1 om de eisen van ons programma te vervullen.
	|	|-- frontEndCode	::De code van alle conversies.
	|	|-- TestFiles		::De test files die worde gebruikt tijdens de presentatie en de verdediging.
	|-- workingDirectory 		::Bevat csv2midi.jar, het JAVA programma dat CSV naar midi files omzet.
	|	|-- midiData		::Instrument- en nootlijsten nodig voor de conversie.

[Ondersteunde conversies]

MusicXML -> MEI
MusicXML -> CSV
MusicXML -> MIDI --via CSV--
MEI -> MusicXML
MEI -> CSV
MEI -> MIDI --via CSV--

[Bijkomende dependencies.]

Om de CSV -> Midi conversie te runnen is JAVA vereist.
Om MusicXML/Midi files door het programma te laten openen en te laten afspelen is musescore vereist (zie http://musescore.org/en/download).

